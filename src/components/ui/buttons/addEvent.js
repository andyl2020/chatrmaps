import React from "react";

const AddEventButton = () => {
  return (
    <div className = "b--add_event" >
      <button type = "button">Add Event</button>
    </div>
  );
}

export default AddEventButton;
